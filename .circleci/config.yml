version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:23.8.0
      - environment:
        TEST_DATABASE_URL: postgres://postgres@localhost:5432/circleci_test
      - image: cimg/postgres:17.3.0
    steps:
      - checkout
      - name: WHO_AM_I
        run: whoami